filtering wireshark
    "apply a display/capture filter"
        display filter (ctrl + /)
        can filter for dns
        can also limit the display packets to a particular ip
            ip.addr==<ip>

note: basically all raw data sent is encrypted

command line network management utility
    ping
        cused to measure latency between user and another location on the internet
        uses icmp (internet control and management protocol)
        // ping actually sends the alphabet as a body
    tracert
        shows the route that a packet would take to reach the destination
        
analysis
    clicking on the breakup of a frame in the bottom left pane will show the size of each section on the bottom of the window, and that will light up the section in the bottom right pane

arp
    traffic that runs directly over layer 2
    determines mac addresses