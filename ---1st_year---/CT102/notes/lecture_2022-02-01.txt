// note: sum of n integers from 1 to n
// \frac{n(n+1)}{2}

// note: formula for sum of n consecutive integers, from k to l
// \frac{n(k + l)}{2}

bubble sort
    O(n^2), one of the worst sorting algorithms for general data

selection sort
    searches the entire array and selects the largest/smallest element, and puts it at the corresponding index
    then, checks for next largest/smallest element (ignoring already sorted index)
    repeat until sorted

    algorithm outline
        input:
            array of integers with give size
        output:
            array with data sorted in increasing order
        approach (for finding smallest):

            void selection_sort(int arr[], int size) {
                int min, temp;

                for (int i = 0; i < size; i++) {
                    min = i;

                    for (int j = min + 1; j < size; j++) {
                        if (arr[min] > arr[j]) {
                            min = j;
                        }
                    }

                    if (i != min) {
                        temp = arr[i];
                        arr[i] = arr[min];
                        arr[min] = temp;
                    }
                }
            }

    algorithm analysis
        also O(n^2)
        now check the comparison in terms of amount of swaps/comparisons -  less than bubble sort
